apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-service.fullname" . }}
  labels:
    {{- include "infra-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-service.selectorLabels" . | nindent 4 }}
